# Production Environment Configuration
# Copy this to .env on the server

# Domain Name (IMPORTANT: Must match DNS)
# Caddy will automatically get SSL for this domain
DOMAIN=stream.volleybratans.com

# Backend Port (Internal Docker Network)
PORT=8080

# Frontend Port (Internal Docker Network)
WEB_PORT=80

# Security (Optional but Recommended)
# If set, clients must send this password to control the stream
# PASSWORD=ChangeThisToSomethingSecret

# Allowed Origins (CORS)
# Add your domain here
ALLOWED_ORIGINS=https://stream.volleybratans.com,http://localhost:3000
